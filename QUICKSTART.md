# 🚀 Быстрый старт

## Шаг 1: Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его (Windows)
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

## Шаг 2: Создание файла .env

Создайте файл `.env` в корне проекта:

```env
TG_TOKEN=8545587906:AAESXhReZ_BC_4R5Hb-2DpOegsE5lExtQF0
ADMIN_ID=7976524871
```

**Важно:** Замените `TG_TOKEN` на ваш токен бота (если нужно).

## Шаг 3: Запуск бота

```bash
python bot.py
```

Вы должны увидеть:
```
Запуск бота через long polling...
Admin ID: 7976524871
Данные хранятся в файле data.json
```

**Всё! База данных не нужна!** Все данные автоматически сохраняются в файл `data.json`.

## Шаг 4: Тестирование

1. Откройте Telegram и найдите вашего бота
2. Отправьте `/start`
3. Нажмите "🎮 Начать игру"
4. Решайте задачи, отправляя ответы
5. Через 60 секунд получите результат

## Проверка админ-команд

С аккаунта с ID `7976524871`:
- `/admin` - показать админ-панель
- `/stats` - общая статистика
- `/users` - список пользователей

## Решение проблем

### Ошибка токена

Проверьте:
- `TG_TOKEN` в `.env` правильный
- Нет лишних пробелов в токене

### Бот не отвечает

- Проверьте `TG_TOKEN` в `.env`
- Убедитесь, что бот запущен (нет ошибок в консоли)
- Проверьте файл `bot.log`

### Ошибки импорта

Убедитесь, что виртуальное окружение активировано и все зависимости установлены:
```bash
pip install -r requirements.txt
```

### Файл data.json

Файл `data.json` создаётся автоматически при первом запуске. Если он повреждён, будет создан новый. Резервная копия сохраняется в `data.json.backup`.

## Преимущества этого подхода

✅ **Простота** - не нужна база данных  
✅ **Надёжность** - автоматические резервные копии  
✅ **Портативность** - один файл со всеми данными  
✅ **Быстрый запуск** - работает сразу после установки зависимостей  
