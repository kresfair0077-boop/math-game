# 🦄 Unicorn Math Game - Telegram Bot

Математическая игра-бот для Telegram, где игроки решают простые арифметические задачи на время (60 секунд).

## 📋 Описание

Бот отправляет подряд простые арифметические задачи (сложение, вычитание, умножение, деление). Ответы — целые числа от 0 до 99. При нажатии «Начать игру» запускается 60-секундный раунд: бот по очереди выдаёт задачи, игрок отвечает в чат — бот сразу даёт следующий пример. По истечении 60 секунд бот выводит результат (сколько задач решено правильно), предлагает сыграть снова и показать таблицу лидеров.

**Все данные хранятся в файле `data.json` - никаких баз данных не требуется!**

## 🛠 Технологии

- **Python 3.13+**
- **python-telegram-bot v20+** (async, long polling)
- **JSON файл** для хранения данных (просто и надёжно)
- **python-dotenv** для конфигурации

## ⚡ Быстрый старт

Для быстрого запуска см. [QUICKSTART.md](QUICKSTART.md)

## 📦 Установка и запуск

### 1. Требования

- Python 3.13 или выше
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

**База данных НЕ требуется!** Все данные хранятся в файле `data.json`.

### 2. Установка зависимостей

```bash
# Создаём виртуальное окружение
python -m venv venv

# Активируем виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

### 3. Настройка конфигурации

Создайте файл `.env` на основе `.env.example`:

```bash
# Windows (PowerShell):
Copy-Item .env.example .env

# Linux/Mac:
cp .env.example .env
```

Отредактируйте `.env` и укажите:

```env
TG_TOKEN=ваш_токен_бота
ADMIN_ID=7976524871
```

**⚠️ ВАЖНО:** Не коммитьте файл `.env` в репозиторий! Он уже добавлен в `.gitignore`.

### 4. Запуск бота

```bash
python bot.py
```

Бот запустится через long polling и начнёт принимать сообщения. Данные будут автоматически сохраняться в файл `data.json`.

## 🎮 Использование

### Команды для пользователей

- `/start` — приветствие и начало работы с ботом
- `/profile` — показать свою статистику

### Игровой процесс

1. Отправьте `/start` боту
2. Нажмите кнопку «🎮 Начать игру»
3. Решайте задачи, отправляя ответы в чат (целые числа)
4. Через 60 секунд получите результат
5. Можете сыграть ещё раз или посмотреть таблицу лидеров

### Команды для администратора

Только пользователь с `ADMIN_ID` из `.env` имеет доступ к админ-командам:

- `/admin` — показать админ-панель
- `/stats` — общая статистика (пользователи, игры, средний счёт)
- `/users` — список пользователей (топ-50)
- `/user <telegram_id>` — детали конкретного пользователя
- `/export_results` — экспорт всех результатов в CSV
- `/leaderboard` — таблица лидеров

## 💾 Хранение данных

Все данные хранятся в файле `data.json` в корне проекта:

- Пользователи (users)
- Результаты игр (game_results)
- Попытки ответов (attempts)

Файл создаётся автоматически при первом запуске. Перед каждым сохранением создаётся резервная копия `data.json.backup`.

**⚠️ Рекомендация:** Регулярно делайте резервные копии файла `data.json`!

## 📁 Структура проекта

```
telegrammbot_math/
├── README.md              # Этот файл
├── QUICKSTART.md          # Быстрый старт
├── .env.example           # Пример конфигурации
├── .gitignore            # Игнорируемые файлы
├── requirements.txt      # Зависимости Python
├── bot.py                # Главный файл запуска
├── config.py             # Конфигурация (загрузка .env)
├── db.py                 # Работа с JSON файлом
├── models.py             # Модели данных (dataclass)
├── game.py               # Логика игры и генерации задач
├── utils.py              # Вспомогательные функции
├── handlers/
│   ├── __init__.py
│   ├── user_handlers.py  # Обработчики для пользователей
│   └── admin_handlers.py # Обработчики для админа
├── data.json             # Файл с данными (создаётся автоматически)
└── bot.log               # Лог-файл (создаётся автоматически)
```

## 🧪 Тестирование

### Ручное тестирование

1. **Проверка запуска:**
   - Запустите бота: `python bot.py`
   - Убедитесь, что в консоли нет ошибок
   - Проверьте, что создался файл `data.json`

2. **Проверка базовых команд:**
   - Отправьте `/start` — должно появиться приветствие и кнопка
   - Нажмите «Начать игру» — должна появиться первая задача
   - Отправьте ответ (число) — должен появиться следующий вопрос
   - Подождите 60 секунд — должно появиться итоговое сообщение

3. **Проверка админ-команд:**
   - С аккаунта админа отправьте `/admin`
   - Проверьте `/stats`, `/users`, `/leaderboard`

4. **Проверка сохранения данных:**
   - После игры проверьте файл `data.json`
   - Убедитесь, что данные сохранились

## 🔒 Безопасность

- **Не коммитьте** файл `.env` в репозиторий
- Храните `TG_TOKEN` в секрете
- Регулярно делайте резервные копии `data.json`
- Админ-команды доступны только пользователю с `ADMIN_ID`

## 🐛 Решение проблем

### Ошибка токена

```
telegram.error.InvalidToken: Invalid token
```

**Решение:** Проверьте, что `TG_TOKEN` в `.env` правильный и не содержит лишних пробелов.

### Бот не отвечает

**Решение:**
- Проверьте логи в `bot.log`
- Убедитесь, что бот запущен
- Проверьте интернет-соединение

### Ошибки с файлом данных

Если файл `data.json` повреждён, он будет автоматически пересоздан. Резервная копия сохраняется в `data.json.backup`.

### Ошибки импорта

Убедитесь, что виртуальное окружение активировано и все зависимости установлены:
```bash
pip install -r requirements.txt
```

## 📝 Логирование

Все события логируются в файл `bot.log` и в консоль. Уровень логирования: `INFO`.

## 🚀 Развёртывание

Для продакшена рекомендуется:

1. Использовать systemd (Linux) или службы Windows для автозапуска
2. Настроить ротацию логов
3. Использовать переменные окружения системы вместо `.env`
4. Регулярно делать резервные копии `data.json`
5. Настроить мониторинг и алерты

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👤 Автор

Создано для Telegram-бота Unicorn Math Game.

---

**Приятной игры! 🦄✨**
